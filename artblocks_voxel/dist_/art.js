let n,e,r,o,t,a;tokenData.hash="0x5637b39f91278a85df462164100e0ee8b24bb786d39edd02ea005133e092feb1",console.log(tokenData.hash);let l,c,i,s,p,f,d,g,b,x,m,u,v,y,F,h,z,_=Math,k=Array,V=_.min,D=_.max,$=_.floor,I=_.abs,j=_.cos,C=_.sin,w=devicePixelRatio,R=location.href.split("#")[1],N=n=>n[0].map((e,r)=>n.slice().reverse().map(n=>n[r])),P=0,E=3,B=4,L=5,S=6,M=7,A=30,q=.95532,O=[...Array(1e3)].map(()=>[...Array(10)].map(()=>[0,0,0])),X=()=>{n=new Uint32Array([4,1,e=o=2,3].map(n=>parseInt(tokenData.hash.substr(8*n,8),16))),r=()=>(o=n[3],n[3]=n[2],n[2]=n[1],n[1]=e=n[0],o^=o<<11,n[0]^=o^o>>>8^e>>>19,n[0]/2**32),t=(n,e)=>n[n.length*r()**(e||1)|0],a=n=>n.map(n=>[n,r()]).sort((n,e)=>n[1]-e[1]).map(n=>n[0]),v=[],u_tick=1e-6,b=[r()**4*2|0,r()**8*2|0,0,5*(1-r()**.3)|0,0,t([2,1],.5),(3*r()|0)-1,0,0,0,0],l=(b[0]-.5)*_.PI/2-_.PI;let x=[[8+2*r()|0,30,5,8,0],[4,3+4*r()|0,0,1,1],[8+2*r()|0,30,3,8,r()**4*8],[6+4*r()|0,10+20*r()|0,2,6,2*r()],[6+(0|r()),10+10*r()|0,0,4,r()**2*3]];b[4]=_.sqrt(1-(r()-1)**2)*x.length|0,[s,p,f,d,g]=x[b[4]],c=2+2*r()|0,u=[],b[2]=r()**.5*8|0,i="".substr(30*b[2],30).match(/(.{2})/g).map(n=>Number("0x"+n)),F=4*r()|0},K=()=>{let n=a([[[2,1,2],[[0,1],[0,1]],0,E],[[2,1,2],0,[[0,1],[0,1]],B],[[2,1,4],0,0,P],[[2,1,3],0,0,P],[[1,1,6],0,0,P],[[1,2,3],0,[[1,0,1]],L],[[1,1,3],0,0,P],[[2,1,2],0,0,P],[[1,1,1],0,0,P],[[1,2,1],0,0,P]].filter(n=>n[0][2]<s)).slice(0,c),e=a([[[1,4,1],[[0]],0,S],[[1,.5,1],[[0]],0,M]]);x=[...k(s)].map(()=>k(s).fill(0)),m=[...k(s)].map(()=>k(s).fill(0));for(let o=0;o<p;o++){let a,l,c,i=0,y=-9e9,F=0,h=t(n);o>=p-g&&(h=t(e,.7),f=6,d=6,F=1);for(let n=0;n<d;n++){c=JSON.parse(JSON.stringify(h)),c[4]=4*r()+1|0,c[5]=4*r()+1|0,2==b[3]&&(c[4]=c[5]=1),c[6]=4*r()|0,1==b[3]&&(c[6]=0),c[7]=1,c[8]=4*r()|0,c[3]==M&&(c[8]=0);let e=()=>k(9).fill(k(9).fill(1));c[2]=c[2]||e(),c[1]=c[1]||e(),c[9]=[...c[0]];for(let n=0;n<c[8];n++)c[9].reverse(),c[2]=N(c[2]),c[1]=N(c[1]),c[7]=!c[7];if(c[9][0]>s/2){d<A&&d++;continue}c[10]=s%2==0?[c[9][0]/2+(r()*(s/2+1-c[9][0])|0),0,-s/2+c[9][2]/2+(r()*(s+1-c[9][2])|0)]:[c[9][0]/2+(r()*((s-1)/2+1-c[9][0])|0)+.5,0,-s/2+c[9][2]/2+(r()*(s+1-c[9][2])|0)],c[9][0]%2==s%2&&r()<1/(s-c[9][0])&&(c[9][0]%2||c[7])&&(c[10][0]=0);let o=0,t=0,p=[...k(c[9][0])].map((n,e)=>c[10][0]+e-(c[9][0]-1)/2),g=[...k(c[9][2])].map((n,e)=>c[10][2]+e-(c[9][2]-1)/2);for(let n of p)for(let e of g)n>=0?t++:o++;let u=0,v=0,F=0,z=0;for(let n of g)for(let e of p){let r=z%c[9][0],o=$(z/c[9][0]);z++,v=D(v,x[e+s/2-.5][n+s/2-.5]),F=D(F,m[e+s/2-.5][n+s/2-.5]),1==c[2][r][o]&&(u=D(u,x[e+s/2-.5][n+s/2-.5]))}u<F||u>v?d<A&&d++:(a=[0,-_.hypot(c[10][0],c[10][2]),-u,-_.hypot(c[10][0],u-10,c[10][2]),-I(_.hypot(c[10][0],u-10,c[10][2])-s),-I(_.hypot(c[10][0],2*u,c[10][2])-s),2*u+c[10][2]][f],(a>y||0==n)&&(y=a,i=u,l=c))}if(l&&(l[10][1]=i+l[0][1]/2,l[10][1])){if(F&&l[10][1]-l[9][1]/2==0)continue;let n=k(l[9][0]).fill().map((n,e)=>l[10][0]+e-(l[9][0]-1)/2),e=k(l[9][2]).fill().map((n,e)=>l[10][2]+e-(l[9][2]-1)/2),r=0;for(let o of e)for(let e of n){let n=r%l[9][0],t=$(r/l[9][0]);r++,x[e+s/2-.5][o+s/2-.5]=i+l[0][1],0==l[1][n][t]&&(x[e+s/2-.5][o+s/2-.5]=-99),m[e+s/2-.5][o+s/2-.5]=i+l[0][1]}u.push(l);for(let n=0;n<l[9][0];n++)for(let e=0;e<l[9][1];e++)for(let r=0;r<l[9][2];r++){let o=l[10][0]-l[9][0]/2+n+5|0,t=l[10][1]-l[9][1]/2+e|0,a=l[10][2]-l[9][2]/2+r+5|0;O[a+10*t][o][0]=O[a+10*t+10][o][1]=255*(u.length+1)/64}for(let n=0;n++<8;){let e=[0,0,0].map((e,r)=>(n>>r&1)-.5);v.push([e[0]*(l[9][0]+2*l[10][0]),e[1]*l[9][1]+l[10][1],e[2]*l[9][2]+l[10][2]])}}}},U=()=>{y=[-99,99,99,-99];let n=(n,e,r)=>[n*j(r)-e*C(r),n*C(r)+e*j(r)];v.forEach(e=>{let[r,o,t]=e;[r,t]=n(r,t,-l),[o,t]=n(o,t,-q),y[2]=V(r,y[2]),y[3]=D(r,y[3]),y[1]=V(o,y[1]),y[0]=D(o,y[0])}),y[4]=y[3]-y[2],y[5]=y[0]-y[1],y[6]=D(y[4]/1.8,y[5]/1.8,1),y[7]=y[2]+y[4]/2,y[8]=y[1]+y[5]/2};X(),K(),U(),console.log(i),u_colors=u.map(n=>[n[4],n[5],n[6]]).flat();let G="";G+=u.map((n,e)=>`ps[${e}]=vec3(${n[10][0]},${n[10][1]},${n[10][2]});`).join(""),G+=u.map((n,e)=>`ss[${e}]=vec3(${n[0][0]},${n[0][1]},${n[0][2]});`).join(""),G+=u.map((n,e)=>`cs[${e}]=ivec3(${n[4]},${n[5]},${n[6]});`).join(""),G+=u.map((n,e)=>`rt[${e}]=vec2(${n[8]},${n[3]});`).join("");let W=_.min(innerWidth,innerHeight)*w,J=document.createElement("canvas");J.style.width=W/w+"px",J.style.height=W/w+"px",W=V(W,2048);let T=J.getContext("webgl",{preserveDrawingBuffer:!0});document.body.appendChild(J);let H=i.slice(3*F,3+3*F);J.style.background=3==b[3]?"#333":`rgb(${H})`,J.width=W,J.height=W;let Q=createREGL({gl:T,extensions:["webgl_draw_buffers","oes_texture_float"]}),Y=Q.texture(O),Z=Q.framebuffer({color:[Q.texture({type:"float",width:W,height:W}),Q.texture({type:"float",width:W,height:W})],depth:!1}),nn=Q.framebuffer({color:[Q.texture({type:"float"}),Q.texture({type:"float"})],depth:!1}),en={frag:`#extension GL_EXT_draw_buffers : require\n                precision highp float;\n                #define BLOCKS_NUMBER_MAX 60\n                #define PI 3.1415\n                #define S smoothstep\n                #define V vec3\n                #define F float\n                #define N normalize\n                #define L length\n                #define v vec2\n                mat2 rot(F a) {→mat2(cos(a),-sin(a),sin(a),cos(a));} // FIXME make define\n                #define rnd(x) fract(54321.987 * sin(987.12345 * x + .1))\n                // #define rot(a) mat2(cos(a),-sin(a),sin(a),cos(a))\n                #define EPS .0001\n                F sabs(F p) {→sqrt(abs(p)*abs(p)+5e-5);}\n                F smax(F a, F b) {→(a+b+sabs(a-b))*.5;}\n                \n                V ps[BLOCKS_NUMBER_MAX];\n                V ss[BLOCKS_NUMBER_MAX];\n                v rt[BLOCKS_NUMBER_MAX];\n                ivec3 cs[BLOCKS_NUMBER_MAX];\n                \n                uniform V pt[5];\n                uniform F aa;\n                uniform F rs;\n                uniform F render;\n\n                uniform sampler2D tex3d;\n                uniform sampler2D texCol;\n                uniform sampler2D texNorm;\n        \n                ivec3 colIds = ivec3(0);\n                F gl;\n                F camDist = 30.;\n                ivec2 blockId;\n                F cornerR = .01, gap = .015, block;\n                F outlineWidth = .015;//((cornerR+gap)*sqrt(2.) - cornerR);\n                \n                // int eye = 0;\n                // V eye = V(0.);\n                // eye = 0;\n    \n                F tube(V p, V s){\n                    v po = v(L(p.xz), p.y - clamp(p.y, EPS, s.x));\n                    po.x -= clamp(po.x, s.z, s.y);\n                    →L(po)-EPS;\n                }\n                \n                F dist(V p) {\n                    // return max(length(p.xz)-.4,abs(p.y)-.9);\n                    // return length(p)-.6;\n\n                    // →length(fract(p)-.5)-.5;\n                    p.x = abs(p.x);\n                    // F res = 1e5;\n                    F res = p.y + 1.; // floor plane\n                    // F res = length(p)-.5; // floor plane\n                    for(int i = 0; i < BLOCKS_NUMBER_MAX; i++) {\n                        if(i >= ${u.length})\n                            break;\n                        if(i != blockId.x - 1 && i != blockId.y - 1)\n                        // if(i != blockId.y - 1)\n                            continue;\n                        V pb = p;\n                        pb -= ps[i];\n                        pb.xz *= rot(rt[i].x * PI / 2.);\n                        \n                        // box\n                        // F cornerR = .01, gap = .008, block;\n                        \n                        V s = ss[i] - 2. * (cornerR + gap);\n                        block = L(pb - clamp(pb, -s/2., s/2.)) - cornerR * 1.4;\n                        // if(blockId==0) {colIds = ivec3(3, 2, 1); return length(fract(p)-.5)-.45;}\n                            \n                        if(rt[i].y == 5.) { // arc\n                            F cyl = L(pb.zy) - .5;\n                            F box = max(abs(pb.z) - .5, abs(pb.y + ss[i].y / 2.) - 1.);\n                            F hole = min(cyl, box);\n                            block = max(block, -hole);\n                        }\n    \n                        if(rt[i].y == 6.) { // pillar\n                            F narrow = tube(pb+V(0,1.6-cornerR*3.,0),V(3.55,.15,0));\n                            F base = tube(pb+V(0,2.-cornerR*2.,0),V(.4-cornerR*2.,.45,0));\n                            block = min(narrow, base);\n                            // // F narrow = tube(pb+V(0,1.6-cornerR-gap,0),V(3.55,.15,0));\n                            // F narrow = tube(pb+V(0,4./2.-.4-cornerR*2.-gap,0),V(3.55,.15-cornerR,0));\n                            // F base = tube(pb+V(0,2.-cornerR*2.,0),V(.4-cornerR*2.-gap,.45,0));\n                            // block = min(narrow, base)-cornerR;\n                        }\n\n                        // studs\n                        if(rt[i].y != 6.) { // not pillar\n                            V ps = pb;\n                            // repetition\n                            v l = ss[i].xz;\n                            ps.xz += (l - 1.) / 2.;\n                            ps.xz = ps.xz - clamp(floor(ps.xz + .5), v(0.), l - 1.);\n                            \n                            // position\n                            ps.y -= ss[i].y / 2. + .02;\n    \n                            F stud = tube(ps, V(.24, .28, mix(EPS,.18,${b[1]}.)));\n                            block = min(stud, block);\n                        }\n                \n                        if(pb.z<.01 && (rt[i].y == 3. || rt[i].y == 4.)) { // beak\n                            block = smax(block,dot(pb,V(0,.78*(7.-2.*rt[i].y),-.624))-.39);\n                        }\n                \n                \n                \n                        if(rt[i].y == 7.) { // eye\n                            // F eye_ = cyl(pb, V(.2, .25, .2), cornerR);\n                            F eye_ = tube(pb+V(0,.25-cornerR*2.,0),V(.4-cornerR*2.,.45,0));\n                            block = eye_;\n                            if(eye_ < EPS) {\n                                colIds.z = 9; // eye\n                                // discard;\n                                // return block;\n                            }\n                        }\n                \n                        // block = L(pb)-2.;\n                        if(block < res) {\n                            if(colIds.z == 9)// FIXME как-то эти ифы упростить, они нужны только чтобы глаза работали.\n                                colIds = ivec3(cs[i].xy, 9);\n                            else\n                                colIds = cs[i];\n                            res = block;\n                        }\n                        if(res < EPS)\n                            break;\n                    }\n                    →res;\n                }\n                \n                V norm(V p) {\n                    // p+=.5;\n                    // →normalize(fract(p));\n                    v e = v(.01, 0.);\n                    →N(V(dist(p + e.xyy) - dist(p - e.xyy), dist(p + e.yxy) - dist(p - e.yxy), dist(p + e.yyx) - dist(p - e.yyx)));\n                }\n\n                vec2 sdfVoxel(vec3 p){\n                    p.xz += fract(float(${s/2}));  // ODD\n                    // p.xz = p.zx;\n                    p.x = abs(p.x);\n                    p.zx -= fract(float(${s/2})); // ODD\n                    // p.x += ${s}.-1.;\n                    // p.z += ${s}.;\n                    p.x += 5.;\n                    p.z += 5.;\n                    p = floor(p+vec3(0,0,.0));\n                    if(p.y < 0.) return v(0);\n                    // p.x -= .5;\n                    // p.z -= .5;\n                    vec3 boundingBox = vec3(10,1000 / 10,10);\n                    if(fract(p/boundingBox) != p/boundingBox) return -v(1.);\n                    vec2 vox, texSize = vec2(boundingBox.x, boundingBox.y*boundingBox.z);\n                    vox.x = p.x;\n                    vox.y = p.z + p.y * 10.;\n                    vec2 voxN = (vox+.5) / texSize;\n                    blockId = ivec2(texture2D(tex3d, voxN).rg * 64.);\n                    // if(blockId == 2) discard;\n                    return vec2(blockId); // is full\n                }\n\n                // vec3 rgb2hsb( in vec3 c ){\n                //     vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n                //     vec4 p = mix(vec4(c.bg, K.wz),\n                //                  vec4(c.gb, K.xy),\n                //                  step(c.b, c.g));\n                //     vec4 q = mix(vec4(p.xyw, c.r),\n                //                  vec4(c.r, p.yzx),\n                //                  step(p.x, c.r));\n                //     float d = q.x - min(q.w, q.y);\n                //     float e = 1.0e-10;\n                //     return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)),\n                //                 d / (q.x + e),\n                //                 q.x);\n                // }\n                \n                // //  Function from Iñigo Quiles\n                // //  https://www.shadertoy.com/view/MsS3Wc\n                // vec3 hsb2rgb( in vec3 c ){\n                //     vec3 rgb = clamp(abs(mod(c.x*6.0+vec3(0.0,4.0,2.0),\n                //                              6.0)-3.0)-1.0,\n                //                      0.0,\n                //                      1.0 );\n                //     rgb = rgb*rgb*(3.0-2.0*rgb);\n                //     return c.z * mix(vec3(1.0), rgb, c.y);\n                // }\n\n                void main() {\n                    if(render == 1.){\n                        gl_FragData[0] = texture2D(texCol, gl_FragCoord.xy/rs);\n                        // return;\n                        V norm = (texture2D(texNorm, gl_FragCoord.xy/rs).rgb);\n                        // norm.y = -norm.y;\n                        // norm.z = -norm.z;\n                        // norm.x = -norm.x;\n                        F dist = texture2D(texNorm, gl_FragCoord.xy/rs).a;\n                        // gl_FragData[0].rgb = vec3(smoothstep(30.,20.,dist));\n                        // gl_FragData[0].a = 1.;\n                        // return;\n                        // gl_FragData[0].rgb =norm;\n                        // gl_FragData[0].rgb =vec3(10./dist);\n                        // gl_FragData[0].a=1.;\n                        // return;\n                        if(dist>camDist*2.) return;\n                        // gl_FragData[0].rgb = rgb2hsb(gl_FragData[0].rgb);\n                        // gl_FragData[0] *= smoothstep(35.,15.,texture2D(texNorm, gl_FragCoord.xy/rs).a);\n                        // vec3 f = V(norm.x,norm.y,norm.z);\n                        // gl_FragData[0].rgb =fract(norm)*.1+.9;\n                        // gl_FragData[0].a=1.;\n                        vec3 f = norm;\n                        // gl_FragData[0].rgb = norm;\n                        // return;\n                        vec3 r = normalize(cross(vec3(1,2,3), f));\n                        vec3 u = cross(f, r);\n                        for(float i=0.; i<100.; i++){\n                            V kernel = V(0,0,0);\n                            kernel += (rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137)))*2.-1.) * r;\n                            kernel += (rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137))+.1)*2.-1.) * u;\n                            kernel += (rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137))+.2)) * f;\n                            kernel *= kernel*kernel;\n                            // kernel /= length(kernel);\n                            // kernel *= (rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137))+.2));\n                            // kernel += f;\n                            // kernel = V(0,1,0);\n                            // return;\n                            // kernel *=  rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137))+.3);\n                            // kernel = V(0.,-0.1,0.);\n                            \n                            vec3 offset = V(dot(kernel,vec3(1,0,0)), dot(kernel,vec3(0,1,0)), dot(kernel,V(0,0,1)));\n                            if(dist - offset.z * 1.1 > texture2D(texNorm, gl_FragCoord.xy/rs + .04 * offset.xy).a){\n                                // gl_FragData[0].b*=.995;\n                                // gl_FragData[0].s*=1.001;\n                                gl_FragData[0].rgb*=.995;\n                            }\n                        }\n                        // gl_FragData[0]/=100.;\n                            // return;\n                        // gl_FragData[0].rgb = hsb2rgb(gl_FragData[0].rgb);\n                        gl_FragData[0].a = 1.;\n                        return;\n                    }\n                    // gl_FragData[0] = vec4(1);\n                    \n                    // // DEBUG\n                    // vec2 uv = (gl_FragCoord.xy * 2. - rs) / rs;\n                    // V ro = V(uv * F(${y[6]}) +\n                    //     v(${y[7]},\n                    //     ${y[8]}), -camDist),\n                    //    rd = V(0, 0, 1);\n                    //    ro.yz *= rot(${q});\n                    //    rd.yz *= rot(${q});\n                    //    ro.xz *= rot(${l});\n                    //    rd.xz *= rot(${l});\n                    // float d,e=1.,j;\n                    // vec3 p;\n                    // for(float i=0.;i<99.;i++){\n                    //     j=i;\n                    //     p=ro+rd*d;\n                    //     p.z-=1.;\n                    //     p.xz = fract(p.xz)-.5;\n                    //     d+=e=dist(p);\n                    //     if(e<1e-3)break;\n                    // }\n                    // gl_FragData[0] = vec4(step(-40.,-d));\n                    // vec3 n = norm(p);\n                    // // n.xz *= -rot(${l});\n                    // // n.xz *= -rot(${l});\n                    // // n.xz *= -rot(${l});\n                    // // n.yz *= rot(${q});\n                    // // n.xy *= rot(.95);\n                    // n.xz *= rot(PI/2. + PI/4.);\n                    // n.xy *= rot(atan(sqrt(2.)));\n                    // n = n.zyx;\n                    // n.x *= -1.;\n                    // n.z *= -1.;\n\n                    // gl_FragData[1] = vec4((n),d);\n                    // return;\n                    // // END OF DEBUG\n\n\n                    ${G}\n                    V o = V(0), n, nnn;\n                    v uv, uvI = (gl_FragCoord.xy * 2. - rs)/rs;\n                    F d;\n        \n                    for(F A = 0.; A < 8.; A++){\n                        if(A >= aa) break;\n                        gl = 0.;\n                        F e = 1e9, ep=9., j; // here highp\n                        d = 0.;\n        \n                        F fl = floor(A/2.);\n                        F fr = mod(A,2.);\n                        v pos = v(fr/2.,fl/4.)-.5;\n                        if(mod(fl, 2.)==0.) pos.x += .25; //https://bit.ly/30g2DXs\n        \n                        v uv = uvI;\n    \n                        uv += pos * 2. / rs;\n        \n                        V n, p, ro = V(uv * F(${y[6]}) +\n                            v(${y[7]},\n                            ${y[8]}), -camDist),\n                           rd = V(0, 0, 1);\n                        bool outline = false;\n\n                        // vec3 ro = vec3(0,0,-10);\n                        // vec3 rd = vec3(0,0,-10);\n                        ro.yz *= rot(${q});\n                        rd.yz *= rot(${q});\n                        ro.xz *= rot(${l});\n                        rd.xz *= rot(${l});\n                        float jj = 0.;\n                        for(float i = 0.; i < 200.; i++) {\n                            jj++;\n                            p = ro + rd * d;\n                            p.xz -= fract(float(${s/2})); // ODD\n                            vec3 dp = (step(0., rd) - fract(p)) / rd;\n                            float dpmin;\n                \n                            dpmin = min(min(dp.x,dp.y),dp.z) + 1e-4;\n                \n                            bool breaker = false;\n                            if(length(sdfVoxel(p)) > 0.) {\n                                float ddd = 0.;\n                                for(float backupI = 0.; backupI < 200.; backupI++) { // FIXME get rid of backupI\n                                    jj++;\n                                    p = ro + rd * (d + ddd);\n                                    ddd += e = dist(p);\n                                    if(ep < e && e < outlineWidth) {\n                                        outline = true;\n                                        breaker = true;\n                                        dpmin = ddd;\n                                        break;\n                                    }\n                                    ep = e;\n                                    if(e < .001 || jj > 200. || d > camDist*2.) { // налетели на сферу\n                                        // if(id > 0.)\n                                        //     col *= color(id);\n                                            // col *= n+.5;\n                                        // if(s > 1.)\n                                            // col *= .6;\n                                        breaker = true;\n                                        dpmin = ddd;\n                                        break;\n                                    }\n                                    if(ddd > dpmin) { // улетели в соседнюю клетку\n                                        break;\n                                    }\n                                }\n                            }\n                            else\n                                colIds = ivec3(0, 0, -1);\n                                d += dpmin;\n                                if(breaker == true || jj > 200.)\n                                break;\n                                \n                        }\n\n                        V c;\n                        if(!outline) {\n                            V col1, col2;\n                            for(int j = 0; j < 5; j++) {\n                                if(colIds[0] == j)\n                                col1 = pt[j];\n                                if(colIds[1] == j)\n                                col2 = pt[j];\n                            }\n                            \n                            V col = col1;\n                            \n                            // Texturing\n                            //\n                            // layers\n                            if(colIds.z == 1)\n                            if(sin(p.y * PI * 3.) > 0.)\n                            col = col2;\n                            if(colIds.z == 2)\n                            if(sin((p.x + fract(ps[0].x - ss[0].x / 2.)) * PI * 2. * 1.5) > 0.)\n                            col = col2;\n                            \n                            // pride\n                            if(${b[3]} == 3)\n                            col = sin((L(p) / max(F(${s}), F(${b[8]})) * 2. - V(0, .3, .6)) * 6.28) * .5 + .5;\n\n                            n = norm(p); // надо тут вычислять, видимо, где-то выше я сбиваю colIds выполняя дист\n                            // иначе colIds.z равен 0 с чего-то. Но почему тогда dist(p); не помогает?\n                            if(colIds.z == 9) {\n                                col = V(0);\n                                V pe = p + fract(${s}. / 2.);\n                                pe = fract(pe) - .5;\n                                col += step(.3, L(pe.xz));\n                                col += step(-.1, -L(pe.xz + .1));\n                            }\n                        \n                            if(colIds.z == -1) {\n                                // // фончик\n                                // c = texture2D(tex3d,gl_FragCoord.xy / rs).rgb;\n                                \n                                c = V(${H})/255.;\n                                if(L(c) > .4){\n                                    c *= S(5., 0., L(uv + v(${b[6]}, -1)));\n                                }\n                                // c = V(1,0,1);\n                                if(${b[3]} == 3){\n                                    c = V(.2);\n                                }\n                                if(sin(L(pow(abs(uv), v(${b[5]}))) * 32.) > 0.)\n                                c *= .95;\n                            } else {\n                                // c = V(1,0,1);\n                                // shading\n                                c = col;\n                                c *= min(1.5, 55. / jj) * .2 + .8;\n                                c *= dot(n, N(V(-.5,.5,0))) * .2 + 1.;\n\n                                // glare\n                                if(colIds.z!=9)\n                                    c += pow(abs(dot(n, N(V(0, 1.5, .5)))), 40.);\n                            }\n                            // gazya\n                            // if(${b[3]} == 4)\n                            //     c = (V(20. / jj));\n                        }\n                        // n = norm(p);\n                        // c = n;\n                        // // texture debug\n                        // c.g = fract(gl_FragCoord.y / rs * 11.);\n                        // c.g *= pow(fract(gl_FragCoord.y / rs * 1000.),8.);// * fract(gl_FragCoord.x / rs * 10.);\n                        // c.g += step(0.001,texture2D(tex3d, gl_FragCoord.xy / rs).r) * 8.;\n                        // c *= 30./jj;\n\n                        o += c;\n                        nnn+=n;\n                    }\n                    gl_FragData[0] = vec4(o/aa,1);\n                    nnn/=aa;\n                    nnn.xz *= rot(PI/2. + PI/4.);\n                    nnn.xy *= rot(atan(sqrt(2.)));\n                    nnn = nnn.zyx;\n                    nnn.x *= -1.;\n                    nnn.z *= -1.;\n                    gl_FragData[1] = vec4(nnn,d);\n                    // gl_FragData[1] = vec4(0,0,1,1);\n                    // return;\n                }`.replace(/@/g,"\n#define ").replace(/→/g,"return "),vert:"attribute vec2 g;void main(){gl_Position=vec4(g,0,1);}",attributes:{g:[[1,1],[1,-4],[-4,1]]},uniforms:{rs:Q.prop("r"),pt:i.map(n=>n/255),aa:Q.prop("a"),tex3d:Y,texCol:nn.color[0],texNorm:nn.color[1],render:0},scissor:{enable:!0,box:{x:Q.prop("x"),y:Q.prop("y"),width:Q.prop("t"),height:Q.prop("t")}},depth:{enable:!1},framebuffer:Z,count:3},rn=Q(en),on=1+(W/z|0),tn=0,an=new Date,ln=1,cn=1;R&&(cn=Number(R));let sn=1;z=256,rn({r:W,x:0,y:0,t:W,a:8}),en.framebuffer=null,en.uniforms.render=1,en.uniforms.texCol=Z.color[0],en.uniforms.texNorm=Z.color[1];let pn=Q(en);pn({r:W,x:0,y:0,t:W,a:1});'tx shvembldr piter stranger'
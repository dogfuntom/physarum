let n,e,r,o,t,c;window.location.hash&&(tokenData.hash=window.location.hash.slice(1));let a,i,l,p,s,d,f,x,b,g,m,v,u,y,k,z,F,h=Math,V=Array,$=h.min,_=h.max,I=h.floor,D=h.abs,w=h.cos,j=h.sin,C=devicePixelRatio,R=location.href.split("#")[1],N=n=>n[0].map((e,r)=>n.slice().reverse().map(n=>n[r])),B=0,E=3,L=4,P=5,S=6,q=7,M=30,O=.95532,X=[...Array(1e3)].map(()=>[...Array(10)].map(()=>[0,0,0])),A=()=>{n=new Uint32Array([4,1,e=o=2,3].map(n=>parseInt(tokenData.hash.substr(8*n,8),16))),r=()=>(o=n[3],n[3]=n[2],n[2]=n[1],n[1]=e=n[0],o^=o<<11,n[0]^=o^o>>>8^e>>>19,n[0]/2**32),t=(n,e)=>n[n.length*r()**(e||1)|0],c=n=>n.map(n=>[n,r()]).sort((n,e)=>n[1]-e[1]).map(n=>n[0]),u=[],u_tick=1e-6,b=[r()**4*2|0,r()**8*2|0,0,5*(1-r()**.3)|0,0,t([2,1],.5),(3*r()|0)-1,0,0,0,0],a=(b[0]-.5)*h.PI/2-h.PI;let g=[[8+2*r()|0,30,5,8,0],[4,3+4*r()|0,0,1,1],[8+2*r()|0,30,3,8,r()**4*8],[6+4*r()|0,10+20*r()|0,2,6,2*r()],[6+(0|r()),10+10*r()|0,0,4,r()**2*3]];b[4]=h.sqrt(1-(r()-1)**2)*g.length|0,[p,s,d,f,x]=g[b[4]],i=2+2*r()|0,v=[],b[2]=r()**.5*8|0,l="".substr(30*b[2],30).match(/(.{2})/g).map(n=>Number("0x"+n)),k=4*r()|0},K=()=>{let n=c([[[2,1,2],[[0,1],[0,1]],0,E],[[2,1,2],0,[[0,1],[0,1]],L],[[2,1,4],0,0,B],[[2,1,3],0,0,B],[[1,1,6],0,0,B],[[1,2,3],0,[[1,0,1]],P],[[1,1,3],0,0,B],[[2,1,2],0,0,B],[[1,1,1],0,0,B],[[1,2,1],0,0,B]].filter(n=>n[0][2]<p)).slice(0,i),e=c([[[1,4,1],[[0]],0,S],[[1,.5,1],[[0]],0,q]]);g=[...V(p)].map(()=>V(p).fill(0)),m=[...V(p)].map(()=>V(p).fill(0));for(let o=0;o<s;o++){let c,a,i,l=0,y=-9e9,k=0,z=t(n);o>=s-x&&(z=t(e,.7),d=6,f=6,k=1);for(let n=0;n<f;n++){i=JSON.parse(JSON.stringify(z)),i[4]=4*r()+1|0,i[5]=4*r()+1|0,2==b[3]&&(i[4]=i[5]=1),i[6]=4*r()|0,1==b[3]&&(i[6]=0),i[7]=1,i[8]=4*r()|0,i[3]==q&&(i[8]=0);let e=()=>V(9).fill(V(9).fill(1));i[2]=i[2]||e(),i[1]=i[1]||e(),i[9]=[...i[0]];for(let n=0;n<i[8];n++)i[9].reverse(),i[2]=N(i[2]),i[1]=N(i[1]),i[7]=!i[7];if(i[9][0]>p/2){f<M&&f++;continue}i[10]=p%2==0?[i[9][0]/2+(r()*(p/2+1-i[9][0])|0),0,-p/2+i[9][2]/2+(r()*(p+1-i[9][2])|0)]:[i[9][0]/2+(r()*((p-1)/2+1-i[9][0])|0)+.5,0,-p/2+i[9][2]/2+(r()*(p+1-i[9][2])|0)],i[9][0]%2==p%2&&r()<1/(p-i[9][0])&&(i[9][0]%2||i[7])&&(i[10][0]=0);let o=0,t=0,s=[...V(i[9][0])].map((n,e)=>i[10][0]+e-(i[9][0]-1)/2),x=[...V(i[9][2])].map((n,e)=>i[10][2]+e-(i[9][2]-1)/2);for(let n of s)for(let e of x)n>=0?t++:o++;let v=0,u=0,k=0,F=0;for(let n of x)for(let e of s){let r=F%i[9][0],o=I(F/i[9][0]);F++,u=_(u,g[e+p/2-.5][n+p/2-.5]),k=_(k,m[e+p/2-.5][n+p/2-.5]),1==i[2][r][o]&&(v=_(v,g[e+p/2-.5][n+p/2-.5]))}v<k||v>u?f<M&&f++:(c=[0,-h.hypot(i[10][0],i[10][2]),-v,-h.hypot(i[10][0],v-10,i[10][2]),-D(h.hypot(i[10][0],v-10,i[10][2])-p),-D(h.hypot(i[10][0],2*v,i[10][2])-p),2*v+i[10][2]][d],(c>y||0==n)&&(y=c,l=v,a=i))}if(a&&(a[10][1]=l+a[0][1]/2,a[10][1])){if(k&&a[10][1]-a[9][1]/2==0)continue;let n=V(a[9][0]).fill().map((n,e)=>a[10][0]+e-(a[9][0]-1)/2),e=V(a[9][2]).fill().map((n,e)=>a[10][2]+e-(a[9][2]-1)/2),r=0;for(let o of e)for(let e of n){let n=r%a[9][0],t=I(r/a[9][0]);r++,g[e+p/2-.5][o+p/2-.5]=l+a[0][1],0==a[1][n][t]&&(g[e+p/2-.5][o+p/2-.5]=-99),m[e+p/2-.5][o+p/2-.5]=l+a[0][1]}v.push(a);for(let n=0;n<a[9][0];n++)for(let e=0;e<a[9][1];e++)for(let r=0;r<a[9][2];r++){let o=a[10][0]-a[9][0]/2+n+5|0,t=a[10][1]-a[9][1]/2+e|0,c=a[10][2]-a[9][2]/2+r+5|0;X[c+10*t][o][0]=X[c+10*t+10][o][1]=255*(v.length+1)/64}for(let n=0;n++<8;){let e=[0,0,0].map((e,r)=>(n>>r&1)-.5);u.push([e[0]*(a[9][0]+2*a[10][0]),e[1]*a[9][1]+a[10][1],e[2]*a[9][2]+a[10][2]])}}}},U=()=>{y=[-99,99,99,-99];let n=(n,e,r)=>[n*w(r)-e*j(r),n*j(r)+e*w(r)];u.forEach(e=>{let[r,o,t]=e;[r,t]=n(r,t,-a),[o,t]=n(o,t,-O),y[2]=$(r,y[2]),y[3]=_(r,y[3]),y[1]=$(o,y[1]),y[0]=_(o,y[0])}),y[4]=y[3]-y[2],y[5]=y[0]-y[1],y[6]=_(y[4]/1.8,y[5]/1.8,1),y[7]=y[2]+y[4]/2,y[8]=y[1]+y[5]/2};A(),K(),U(),u_colors=v.map(n=>[n[4],n[5],n[6]]).flat();let G="";G+=v.map((n,e)=>`ps[${e}]=vec3(${n[10][0]},${n[10][1]},${n[10][2]});`).join(""),G+=v.map((n,e)=>`ss[${e}]=vec3(${n[0][0]},${n[0][1]},${n[0][2]});`).join(""),G+=v.map((n,e)=>`cs[${e}]=ivec3(${n[4]},${n[5]},${n[6]});`).join(""),G+=v.map((n,e)=>`rt[${e}]=vec2(${n[8]},${n[3]});`).join("");let W=h.min(innerWidth,innerHeight)*C,T=document.createElement("canvas");T.style.width=W/C+"px",T.style.height=W/C+"px",W=$(W,2048);let J=T.getContext("webgl",{preserveDrawingBuffer:!0});document.body.appendChild(T);let H=l.slice(3*k,3+3*k);T.style.background=3==b[3]?"#333":`rgb(${H})`,T.width=W,T.height=W;let Q=createREGL({gl:J,extensions:["webgl_draw_buffers","oes_texture_float"]}),Y=Q.texture(X),Z=[1,1].map(()=>Q.framebuffer({color:[Q.texture({type:"float",width:W,height:W}),Q.texture({type:"float",width:W,height:W})],depth:!1})),nn={frag:`#extension GL_EXT_draw_buffers : require\n                precision highp float;\n                #define BLOCKS_NUMBER_MAX 60\n                #define PI 3.1415\n                #define S smoothstep\n                #define V vec3\n                #define F float\n                #define N normalize\n                #define L length\n                #define v vec2\n                mat2 rot(F a) {→mat2(cos(a),-sin(a),sin(a),cos(a));} // FIXME make define\n                #define rnd(x) fract(54321.987 * sin(987.12345 * x + .1))\n                // #define rot(a) mat2(cos(a),-sin(a),sin(a),cos(a))\n                #define EPS .0001\n                F sabs(F p) {→sqrt(abs(p)*abs(p)+5e-5);}\n                F smax(F a, F b) {→(a+b+sabs(a-b))*.5;}\n                \n                V ps[BLOCKS_NUMBER_MAX];\n                V ss[BLOCKS_NUMBER_MAX];\n                v rt[BLOCKS_NUMBER_MAX];\n                ivec3 cs[BLOCKS_NUMBER_MAX];\n                \n                uniform V pt[5];\n                uniform F tk;\n                uniform F rs;\n\n                uniform sampler2D tex3d;\n                uniform sampler2D texCol;\n                uniform sampler2D texNorm;\n        \n                ivec3 colIds = ivec3(0);\n                F gl;\n                F camDist = 30.;\n                ivec2 blockId;\n                F cornerR = .01, gap = .015, block;\n                F outlineWidth = .015;//((cornerR+gap)*sqrt(2.) - cornerR);\n                \n                // int eye = 0;\n                // V eye = V(0.);\n                // eye = 0;\n    \n                F tube(V p, V s){\n                    v po = v(L(p.xz), p.y - clamp(p.y, EPS, s.x));\n                    po.x -= clamp(po.x, s.z, s.y);\n                    →L(po)-EPS;\n                }\n                \n                F dist(V p) {\n                    // return max(length(p.xz)-.4,abs(p.y)-.9);\n                    // colIds = ivec3(1,1,1);\n                    // return length(fract(p)-.5)-.45;\n\n                    // →length(fract(p)-.5)-.5;\n                    p.x = abs(p.x);\n                    // F res = 1e5;\n                    F res = p.y + 1.; // floor plane\n                    // F res = length(p)-.5; // floor plane\n                    // for(int i = 0; i < 3; i++) {\n                        int i = 0;\n                        // if(i >= ${v.length})\n                        //     break;\n                            // if(i != blockId.y - 1)\n                        if(i != blockId.x - 1 && i != blockId.y - 1) return res;\n                        //     continue;\n                        V pb = p;\n                        pb -= ps[0];\n                        pb.xz *= rot(rt[0].x * PI / 2.);\n                        \n                        // box\n                        // F cornerR = .01, gap = .008, block;\n                        \n                        V s = ss[0] - 2. * (cornerR + gap);\n                        block = L(pb - clamp(pb, -s/2., s/2.)) - cornerR * 1.4;\n                        // if(blockId==0) {colIds = ivec3(3, 2, 1); return length(fract(p)-.5)-.45;}\n                            \n                        if(rt[0].y == 5.) { // arc\n                            F cyl = L(pb.zy) - .5;\n                            F box = max(abs(pb.z) - .5, abs(pb.y + ss[0].y / 2.) - 1.);\n                            F hole = min(cyl, box);\n                            block = max(block, -hole);\n                        }\n    \n                        if(rt[0].y == 6.) { // pillar\n                            F narrow = tube(pb+V(0,1.6-cornerR*3.,0),V(3.55,.15,0));\n                            F base = tube(pb+V(0,2.-cornerR*2.,0),V(.4-cornerR*2.,.45,0));\n                            block = min(narrow, base);\n                            // // F narrow = tube(pb+V(0,1.6-cornerR-gap,0),V(3.55,.15,0));\n                            // F narrow = tube(pb+V(0,4./2.-.4-cornerR*2.-gap,0),V(3.55,.15-cornerR,0));\n                            // F base = tube(pb+V(0,2.-cornerR*2.,0),V(.4-cornerR*2.-gap,.45,0));\n                            // block = min(narrow, base)-cornerR;\n                        }\n\n                        // studs\n                        if(rt[0].y != 6.) { // not pillar\n                            V ps = pb;\n                            // repetition\n                            v l = ss[0].xz;\n                            ps.xz += (l - 1.) / 2.;\n                            ps.xz = ps.xz - clamp(floor(ps.xz + .5), v(0.), l - 1.);\n                            \n                            // position\n                            ps.y -= ss[0].y / 2. + .02;\n    \n                            F stud = tube(ps, V(.24, .28, mix(EPS,.18,${b[1]}.)));\n                            block = min(stud, block);\n                        }\n                \n                        if(pb.z<.01 && (rt[0].y == 3. || rt[0].y == 4.)) { // beak\n                            block = smax(block,dot(pb,V(0,.78*(7.-2.*rt[0].y),-.624))-.39);\n                        }\n                \n                \n                \n                        if(rt[0].y == 7.) { // eye\n                            // F eye_ = cyl(pb, V(.2, .25, .2), cornerR);\n                            F eye_ = tube(pb+V(0,.25-cornerR*2.,0),V(.4-cornerR*2.,.45,0));\n                            block = eye_;\n                            if(eye_ < EPS) {\n                                colIds.z = 9; // eye\n                                // discard;\n                                // return block;\n                            }\n                        }\n                \n                        // block = L(pb)-2.;\n                        if(block < res) {\n                            if(colIds.z == 9)// FIXME как-то эти ифы упростить, они нужны только чтобы глаза работали.\n                                colIds = ivec3(cs[0].xy, 9);\n                            else\n                                colIds = cs[0];\n                            res = block;\n                        }\n                        // if(res < EPS)\n                        //     break;\n                    // }\n                    →res;\n                }\n                \n                V norm(V p) {\n                    // p+=.5;\n                    // →normalize(fract(p));\n                    v e = v(.01, 0.);\n                    →N(V(dist(p + e.xyy) - dist(p - e.xyy), dist(p + e.yxy) - dist(p - e.yxy), dist(p + e.yyx) - dist(p - e.yyx)));\n                }\n\n                void sdfVoxel(vec3 p){\n                    // blockId *= 0;\n                    // p.xz += fract(float(${p/2}));  // ODD\n                    // p.x = abs(p.x);\n                    // p.zx -= fract(float(${p/2})); // ODD\n                    // p.x += 5.;\n                    // p.z += 5.;\n                    // p = floor(p+vec3(0,0,0));\n                    // if(p.y < 0.) return v(0);\n                    // vec3 boundingBox = vec3(10,1000 / 10,10);\n                    // if(fract(p/boundingBox) != p/boundingBox) return v(0);\n                    // blockId = ivec2(max(0., 3.-length(p)));\n                    // return vec2(blockId);\n\n\n                    blockId *= 0;\n                    p.xz += fract(float(${p/2}));  // ODD\n                    // p.xz = p.zx;\n                    p.x = abs(p.x);\n                    p.zx -= fract(float(${p/2})); // ODD\n                    // p.x += ${p}.-1.;\n                    // p.z += ${p}.;\n                    p.x += 5.;\n                    p.z += 5.;\n                    p = floor(p+vec3(0,0,0));\n                    if(p.y < 0.) return; // ←←←←←←←←←←←←←←←←←←←←←←←←\n                    // p.x -= .5;\n                    // p.z -= .5;\n                    vec3 boundingBox = vec3(10,1000 / 10,10);\n                    if(fract(p/boundingBox) != p/boundingBox) return;  // ←←←←←←←←←←←←←←←←←←←←←←←←\n                    vec2 vox, texSize = vec2(boundingBox.x, boundingBox.y*boundingBox.z);\n                    vox.x = p.x;\n                    vox.y = p.z + p.y * 10.;\n                    vec2 voxN = (vox+.5) / texSize;\n                    blockId = ivec2(texture2D(tex3d, voxN).rg * 64.);\n                    // if(blockId == 2) discard;\n                    return;  // ←←←←←←←←←←←←←←←←←←←←←←←←\n                }\n\n                // vec3 rgb2hsb( in vec3 c ){\n                //     vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n                //     vec4 p = mix(vec4(c.bg, K.wz),\n                //                  vec4(c.gb, K.xy),\n                //                  step(c.b, c.g));\n                //     vec4 q = mix(vec4(p.xyw, c.r),\n                //                  vec4(c.r, p.yzx),\n                //                  step(p.x, c.r));\n                //     float d = q.x - min(q.w, q.y);\n                //     float e = 1.0e-10;\n                //     return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)),\n                //                 d / (q.x + e),\n                //                 q.x);\n                // }\n                \n                // //  Function from Iñigo Quiles\n                // //  https://www.shadertoy.com/view/MsS3Wc\n                // vec3 hsb2rgb( in vec3 c ){\n                //     vec3 rgb = clamp(abs(mod(c.x*6.0+vec3(0.0,4.0,2.0),\n                //                              6.0)-3.0)-1.0,\n                //                      0.0,\n                //                      1.0 );\n                //     rgb = rgb*rgb*(3.0-2.0*rgb);\n                //     return c.z * mix(vec3(1.0), rgb, c.y);\n                // }\n\n                void main() {\n                    // gl_FragData[0] = vec4(1);\n                    \n                    // // DEBUG\n                    // vec2 uv = (gl_FragCoord.xy * 2. - rs) / rs;\n                    // V ro = V(uv * F(${y[6]}) +\n                    //     v(${y[7]},\n                    //     ${y[8]}), -camDist),\n                    //    rd = V(0, 0, 1);\n                    //    ro.yz *= rot(${O});\n                    //    rd.yz *= rot(${O});\n                    //    ro.xz *= rot(${a});\n                    //    rd.xz *= rot(${a});\n                    // float d,e=1.,j;\n                    // vec3 p;\n                    // for(float i=0.;i<99.;i++){\n                    //     j=i;\n                    //     p=ro+rd*d;\n                    //     p.z-=1.;\n                    //     p.xz = fract(p.xz)-.5;\n                    //     d+=e=dist(p);\n                    //     if(e<1e-3)break;\n                    // }\n                    // gl_FragData[0] = vec4(step(-40.,-d));\n                    // vec3 n = norm(p);\n                    // // n.xz *= -rot(${a});\n                    // // n.xz *= -rot(${a});\n                    // // n.xz *= -rot(${a});\n                    // // n.yz *= rot(${O});\n                    // // n.xy *= rot(.95);\n                    // n.xz *= rot(PI/2. + PI/4.);\n                    // n.xy *= rot(atan(sqrt(2.)));\n                    // n = n.zyx;\n                    // n.x *= -1.;\n                    // n.z *= -1.;\n\n                    // gl_FragData[1] = vec4((n),d);\n                    // return;\n                    // // END OF DEBUG\n\n\n                    ${G}\n                    V o = V(0), n, nnn;\n                    v uv = (gl_FragCoord.xy * 2. - rs)/rs;\n                    F d;\n        \n                    gl = 0.;\n                    F e = 1e9, ep=9., j; // here highp\n                    d = 0.;\n    \n                    F fl = floor(tk/2.);\n                    F fr = mod(tk,2.);\n                    v pos = v(fr/2.,fl/4.)-.5;\n                    if(mod(fl, 2.)==0.) pos.x += .25; //https://bit.ly/30g2DXs\n    \n                    uv += pos * 2. / rs;\n    \n                    V p, ro = V(uv * F(${y[6]}) +\n                        v(${y[7]},\n                        ${y[8]}), -camDist),\n                        rd = V(0, 0, 1);\n                    bool outline = false;\n\n                    // vec3 ro = vec3(0,0,-10);\n                    // vec3 rd = vec3(0,0,-10);\n                    ro.yz *= rot(${O});\n                    rd.yz *= rot(${O});\n                    ro.xz *= rot(${a});\n                    rd.xz *= rot(${a});\n                    float jj = 0.;\n\n\n\n                    for(float i = 0.; i < 200.; i++) {\n                        jj++;\n                        p = ro + rd * d;\n                        p.xz -= fract(float(${p/2})); // ODD\n                        vec3 dp = (step(0., rd) - fract(p)) / rd;\n                        float dpmin;\n            \n                        dpmin = min(min(dp.x,dp.y),dp.z) + 1e-4;\n\n            \n                        bool breaker = false;\n                        sdfVoxel(p);\n                        // blockId = ivec2(2);\n                        if(length(v(blockId)) > 0. && p.y >= 0.) {\n                        // if(length(p)-5. < 0.) {\n                            float ddd = 0.;\n                            for(float backupI = 0.; backupI < 200.; backupI++) { // FIXME get rid of backupI\n                                jj++;\n                                p = ro + rd * (d + ddd);\n                                ddd += e = dist(p);\n                                if(ep < e && e < outlineWidth) {\n                                    outline = true;\n                                    breaker = true;\n                                    dpmin = ddd;\n                                    break;\n                                }\n                                ep = e;\n                                if(e < .001 || jj > 200. || d > camDist*2.) { // налетели на сферу\n                                    // discard;\n                                    // if(id > 0.)\n                                    //     col *= color(id);\n                                        // col *= n+.5;\n                                    // if(s > 1.)\n                                        // col *= .6;\n                                    breaker = true;\n                                    dpmin = ddd;\n                                    break;\n                                }\n                                if(ddd > dpmin) { // улетели в соседнюю клетку\n                                    break;\n                                }\n                            }\n\n                            // breaker = true;\n                            // colIds = ivec3(1,1,1);\n                            // break;\n                        }\n                        else{\n                            colIds = ivec3(0, 0, -1);\n                        }\n                        d += dpmin;\n                        if(breaker == true || jj > 200.)\n                        break;\n                            \n                    }\n\n                    V c;\n                    if(!outline) {\n                        V col1, col2;\n                        for(int j = 0; j < 5; j++) {\n                            if(colIds[0] == j)\n                            col1 = pt[j];\n                            if(colIds[1] == j)\n                            col2 = pt[j];\n                        }\n                        \n                        V col = col1;\n                        \n                        // Texturing\n                        //\n                        // layers\n                        if(colIds.z == 1)\n                        if(sin(p.y * PI * 3.) > 0.)\n                        col = col2;\n                        if(colIds.z == 2)\n                        if(sin((p.x + fract(ps[0].x - ss[0].x / 2.)) * PI * 2. * 1.5) > 0.)\n                        col = col2;\n                        \n                        // pride\n                        if(${b[3]} == 3)\n                        col = sin((L(p) / max(F(${p}), F(${b[8]})) * 2. - V(0, .3, .6)) * 6.28) * .5 + .5;\n\n                        n = norm(p); // надо тут вычислять, видимо, где-то выше я сбиваю colIds выполняя дист\n                        // иначе colIds.z равен 0 с чего-то. Но почему тогда dist(p); не помогает?\n                        if(colIds.z == 9) {\n                            col = V(0);\n                            V pe = p + fract(${p}. / 2.);\n                            pe = fract(pe) - .5;\n                            col += step(.3, L(pe.xz));\n                            col += step(-.1, -L(pe.xz + .1));\n                        }\n                    \n                        if(colIds.z == -1) {\n                            // // фончик\n                            // c = texture2D(tex3d,gl_FragCoord.xy / rs).rgb;\n                            \n                            c = V(${H})/255.;\n                            if(L(c) > .4){\n                                c *= S(5., 0., L(uv + v(${b[6]}, -1)));\n                            }\n                            // c = V(1,0,1);\n                            if(${b[3]} == 3){\n                                c = V(.2);\n                            }\n                            if(sin(L(pow(abs(uv), v(${b[5]}))) * 32.) > 0.)\n                            c *= .95;\n                        } else {\n                            // c = V(1,0,1);\n                            // shading\n                            c = col;\n                            c *= min(1.5, 55. / jj) * .2 + .8;\n                            c *= dot(n, N(V(-.5,.5,0))) * .2 + 1.;\n\n                            // glare\n                            if(colIds.z!=9)\n                                c += pow(abs(dot(n, N(V(0, 1.5, .5)))), 40.);\n                        }\n                        // gazya\n                        // if(${b[3]} == 4)\n                        //     c = (V(20. / jj));\n                    }\n                    // n = norm(p);\n                    // c = n;\n                    // // texture debug\n                    // c.g = fract(gl_FragCoord.y / rs * 11.);\n                    // c.g *= pow(fract(gl_FragCoord.y / rs * 1000.),8.);// * fract(gl_FragCoord.x / rs * 10.);\n                    // c.g += step(0.001,texture2D(tex3d, gl_FragCoord.xy / rs).r) * 8.;\n                    // c *= 30./jj;\n\n                    // o += c;\n                    // nnn+=n;\n\n\n                    if(tk > 1.){\n                        V norm = (texture2D(texNorm, gl_FragCoord.xy/rs).rgb);\n                        F dist = texture2D(texNorm, gl_FragCoord.xy/rs).a;\n                        dist = min(dist,camDist*2.);\n                        vec3 f = norm;\n                        vec3 r = normalize(cross(vec3(1,2,3), f));\n                        vec3 u = cross(f, r);\n                        for(float i=0.; i<20.; i++){\n                            V kernel = V(0,0,0);\n                            kernel += (rnd(i+tk+dot(uv*99.,vec2(.319,.137)))*2.-1.) * r;\n                            kernel += (rnd(i+tk+dot(uv*99.,vec2(.319,.137))+.1)*2.-1.) * u;\n                            kernel += (rnd(i+tk+dot(uv*99.,vec2(.319,.137))+.2)) * f;\n                            kernel = N(kernel) * pow(rnd(i+dot(mod(gl_FragCoord.xy,10.1*PI),vec2(.319,.137))),2.);\n                            vec3 offset = V(dot(kernel,vec3(1,0,0)), dot(kernel,vec3(0,1,0)), dot(kernel,V(0,0,1)));\n                            if(dist - offset.z * 1.1 > texture2D(texNorm, gl_FragCoord.xy/rs + .15 * offset.xy).a){\n                                // gl_FragData[0] *= .99;\n                                c*=.97;\n                            }\n                        }\n                    }\n\n\n                    gl_FragData[0] = mix(texture2D(texCol, gl_FragCoord.xy/rs), c.rgbb, 1. / tk);\n                    // n.xz *= rot(PI/2. + PI/4.);\n                    n.xz *= -sign(${b[0]-.5})*rot(${a});\n                    n.xy *= rot(atan(sqrt(2.)));\n                    n = n.zyx;\n                    n.xz *= -1.;\n                    gl_FragData[1] = mix(texture2D(texNorm, gl_FragCoord.xy/rs), vec4(n.rgb,d), 1. / tk);\n\n\n                    // gl_FragData[0] = vec4(tk/8.);\n                    // gl_FragData[0].r = sin(length(gl_FragCoord.xy)/tk);\n\n                }\n                \n                \n                \n                \n                \n                \n                `.replace(/@/g,"\n#define ").replace(/→/g,"return "),vert:"attribute vec2 g;void main(){gl_Position=vec4(g,0,1);}",attributes:{g:[[1,1],[1,-4],[-4,1]]},uniforms:{rs:Q.prop("r"),pt:l.map(n=>n/255),tex3d:Y,tk:({tick:n})=>n,texCol:({tick:n})=>Z[n%2].color[0],texNorm:({tick:n})=>Z[n%2].color[1]},depth:{enable:!1},framebuffer:({tick:n})=>Z[(n+1)%2],count:3},en=Q({frag:"#extension GL_EXT_draw_buffers : require\n                precision highp float;\n                uniform float rs;\n                uniform sampler2D texCol;\n                // uniform sampler2D texNorm;        \n                void main() {\n                    gl_FragData[0] = texture2D(texCol,gl_FragCoord.xy/rs);\n                    gl_FragData[0].a = 1.;\n                }",vert:"attribute vec2 g;void main(){gl_Position=vec4(g,0,1);}",attributes:{g:[[1,1],[1,-4],[-4,1]]},uniforms:{rs:Q.prop("r"),texCol:({tick:n})=>Z[(n+1)%2].color[0]},depth:{enable:!1},count:3}),rn=Q(nn),on=1+(W/F|0),tn=new Date,cn=1,an=1;R&&(an=Number(R));let ln=1;F=256;let pn=Q.frame(({tick:n})=>{rn({r:W}),en({r:W}),n>1&&(document.title="👾",pn.cancel())});'tx shvembldr piter stranger'